import { describe, it } from "vite-plus/test";

describe("🎯 Failure Localization Scenarios", () => {
  it.todo("SDK red + Hurl green → adapter bug");
  it.todo("GQL red + SDK green → engine resolver bug");
  it.todo("CONNECT-TS red + GRPCURL green → TS transport bug");
  it.todo("All outer red + all inner green → harness/JWT parity bug");
  it.todo("All paths red → schema/seed data issue");
});
