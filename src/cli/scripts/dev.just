# Future body: cargo run
[doc('Run CLI application')]
[group('dev')]
run:
    @echo "run: cli crate not implemented; skipping"

# Future body: cargo build
[doc('Build CLI application')]
[group('dev')]
build:
    @echo "build: cli crate not implemented; skipping"
