# Client deploy requires Tauri/podman configuration and a client compose file.
[doc('Deploy client application')]
[group('deploy')]
deploy:
    error make {msg: "Client deploy is not implemented because Tauri configuration and client compose files are missing."}
